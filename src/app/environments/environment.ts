// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCjW4nuX822B2_RvZUkw6uOTgfdjIunKU8",
    authDomain: "tindio-8e3fd.firebaseapp.com",
    databaseURL: "https://tindio-8e3fd.firebaseio.com",
    projectId: "tindio-8e3fd",
    storageBucket: "tindio-8e3fd.appspot.com",
    messagingSenderId: "363502929849",
    appId: "1:363502929849:web:47fb4f1a56149f90295868"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
